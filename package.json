{
  "name": "obama-medal-action",
  "version": "1.0.0",
  "description": "Github Actions for Obama meme image",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build src/action.js -o dist"
  },
  "keywords": [
    "meme"
  ],
  "author": "Shriram",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "@vercel/ncc": "^0.33.3"
  }
}
